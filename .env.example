# --------------------------------------------------------------------
# Copy this file to `.env` (same directory) and fill in your API key.
# Never commit your actual `.env` file — it is excluded by .gitignore.
#
# Request a free FRED API key at:
#   https://fred.stlouisfed.org/docs/api/api_key.html
# --------------------------------------------------------------------

FRED_API_KEY=your_32_character_api_key_here
